[phases.setup]
aptPkgs = ["openssl", "libssl-dev", "ca-certificates"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npx prisma generate", "npm run build", "ls -la dist/"]

[start]
cmd = "npx prisma generate && npm run build && node dist/server.js"
